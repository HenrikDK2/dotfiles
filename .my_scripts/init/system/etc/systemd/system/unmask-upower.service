[Unit]
Description=Unmask and start upower after boot
After=multi-user.target

[Service]
Type=oneshot
ExecStart=/bin/bash -c 'systemctl unmask upower.service && systemctl start upower.service'

[Install]
WantedBy=multi-user.target
